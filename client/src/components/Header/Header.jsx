import './Header.scss';

import Logo from '../Logo/Logo';

const Header = () => {
    return (
        <header className='app-header'>
            <Logo></Logo>
        </header>
    );
};

export default Header;
